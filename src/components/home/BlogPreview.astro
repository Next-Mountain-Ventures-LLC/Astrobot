---
import { getPosts } from "@/lib/wordpress";
import BlogCarousel from "./BlogCarousel";

// Fetch the latest 6 blog posts at build time to ensure we have enough for the carousel
const posts = await getPosts(1, 6);
---

{posts.length > 0 && (
  <BlogCarousel posts={posts} client:load />
)}